{
    "configurations": [
        {
            "type": "node-terminal",
            "name": "build",
            "request": "launch",
            "command": "npm run build",
            "cwd": "${workspaceFolder}"
        }
    ]
}